# 浏览器页面翻译插件 — 设计提要

日期：2026-08-29（v2：改为替换式 + 选择窗交互）

## 1. 项目目标

一款基于 **Manifest V3** 的 Chromium 浏览器（Chrome / Edge）翻译扩展：

- **替换式整页翻译**：翻译后译文直接替换原文显示
- **选择窗**：点击页面悬浮按钮弹出，可切换翻译引擎（免费接口 / LLM）与显示模式（替换 / 双语），选择即保存
- **快捷键 `Alt+T`**：按当前选择直接翻译整页

## 2. 交互与功能范围（已确认）

| 交互 / 功能 | 说明 |
|---|---|
| 悬浮按钮 | 单击弹出选择窗；选择窗关闭状态下再单击 = 翻译当前页 |
| 快捷键 Alt+T | 直接按当前选择翻译整页 |
| 选择窗 | 引擎（免费接口 / LLM）、显示模式（替换 / 双语）两项选择；选「LLM」时展开配置组（API 地址 / API Key / 模型名）；选完自动保存并关闭，**不触发翻译** |
| 替换模式 | 页面只显示译文；原文存 `data-original` 属性（供还原与双语显示） |
| 双语模式 | 译文正常显示，原文以灰色小字同时显示 |
| 划词翻译 | 鼠标选中文本后弹出悬浮窗显示译文 |
| 设置面板 | 目标语言（默认中文）、忽略站点列表 |

## 3. 技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 扩展规范 | Manifest V3，JS 原生无框架 | 目标仅为 Chromium，一套代码 |
| 引擎一 | LLM（DeepSeek / OpenAI 兼容 API，默认 `deepseek-chat`） | 质量高、国内直连、成本极低 |
| 引擎二 | MyMemory `api.mymemory.translated.net`（无需 Key） | 国内可用、零成本、已实测（并发 5 稳定）；匿名日配额约 5000 字符 |
| 引擎策略 | 选择窗设定**首选引擎**，失败自动降级另一引擎 | 兼顾质量与可用性 |
| 存储 | `chrome.storage.local`（设置）+ 内存 Map（译文缓存） | 设置持久化，缓存仅会话级 |

## 4. 架构与模块

```
BrowserTranslator/
├── manifest.json          # MV3 声明、权限、content script、commands 快捷键
├── background.js          # 翻译管线统一入口：首选引擎 → 降级引擎；持有 API Key
├── content.js             # 整页翻译核心：文本提取、批量翻译、替换式 DOM 改写、还原、MutationObserver
├── ui.js                  # 悬浮按钮（单击弹选择窗 / 翻译）、选择窗、划词浮窗、快捷键响应
├── content.css            # 注入样式（双语灰字、浮窗、选择窗等）
├── options.html / options.js  # 设置面板（目标语言、忽略站点）
└── DESIGN.md
```

**请求链路**：content → `chrome.runtime.sendMessage` → background 发 fetch → 返回译文。
所有翻译请求统一经 background 发起：既解决 content script 的 CORS 限制，也保证 API Key 不出现在页面上下文。

## 5. 关键设计决策

1. **替换式实现**：文本节点替换为 `<span class="bt-trans" data-original="原文">译文</span>`。原文只存属性、不生成隐藏节点——页面复制/剪贴板不残留原文。
2. **双语 / 替换切换零成本**：根元素切 `bt-dual` class，CSS 一条规则 `html.bt-dual .bt-trans::before { content: attr(data-original); … }` 显示原文灰字；切换不重翻译、不动 DOM。
3. **还原原文（切换式）**：页面已翻译时再次触发（Alt+T / 按钮）= 从 `data-original` 恢复原文文本节点；已还原时再次触发 = 重新翻译。
4. **引擎降级策略**：background 暴露 `translate(texts, from, to, preferred)` 一个入口，按 首选 → 降级 顺序尝试；超时（AbortController 约 10s）、限流、网络失败即降级；两者皆失败保留原文并在按钮上提示。
5. **批量翻译**：文本节点按批次合并请求（默认每批 20 段），一次往返返回整批译文；单段失败只跳过该段，不影响全页。
6. **会话级缓存**：内存 Map 缓存「原文 → 译文」，重复触发命中缓存不产生新请求。
7. **免翻区域**：`script / style / pre / textarea / input / contenteditable` 及已带翻译标记的节点一律跳过。
8. **SPA 适配**：MutationObserver 监听新增节点，节流（约 300ms）后增量翻译。
9. **Key 安全**：API Key 仅存 `chrome.storage.local`、仅 background 读取使用。
10. **设置实时生效**：content 启动读一次 storage，监听 `onChanged`；选择窗的引擎/模式选择持久化，下次打开沿用。

## 6. 实施计划（步骤 → 验收标准）

| 步骤 | 内容 | 验收标准 |
|---|---|---|
| 1. 项目骨架 | manifest.json + 空脚本 + content.css | Edge 开发者模式加载无报错，图标出现 |
| 2. 翻译管线 | background 实现 `translate()`：双引擎 + 首选/降级逻辑 | 两引擎分别返回正确译文；未配 LLM Key 时自动走免费接口；模拟免费接口失败时 LLM 兜底 |
| 3. 整页翻译 | content.js 替换式改写（`bt-trans` + `data-original`）、批量翻译、还原、MutationObserver | 英文页完整替换为中文；代码/输入框不被翻译；`data-original` 有原文；再次触发还原原文；SPA 滚动新内容被翻译 |
| 4. 选择窗 + 设置面板 | 引擎/模式二选并持久化；目标语言、忽略站点 | 选择保存后重启扩展仍生效；忽略站点不触发；设置面板改动实时生效 |
| 5. 交互入口 | 悬浮按钮（弹窗/翻译两态）、Alt+T、划词浮窗 | 单击弹选择窗、关闭后再单击翻译；Alt+T 翻译/还原切换；划词出浮窗；三者互不冲突 |
| 6. 回归收尾 | 全量验收项 + 双语模式 + 性能检查 | 双语模式原文灰字同显、切换零延迟；复制页面无原文残留；长页不卡顿、无重复请求 |

## 7. 待确认事项

- **LLM 服务商（用户自配）**：面板中填写 API 地址 / API Key / 模型名，三者均为自由输入——模型名由用户自选，API 地址与 Key 由用户自备（OpenAI 兼容接口即可，支持任意服务商或本地模型；DeepSeek 地址仅为示例预填，可清空）。
- **免费引擎**：MyMemory（无需 Key，并发 5 已实测 10/10；有道智云 demo 限流 411、Bing ttranslatev3 触发验证码、Google 被墙、Edge 认证不通，均弃用）。限制：需指定源语言（启发式：含中日韩字符→zh，否则→en，覆盖常见英译中，其他源语言请用 LLM）、匿名日配额约 5000 字符、无 Key 接口有失效风险；失效/超配额时自动降级 LLM 或保留原文。
- **还原语义**：已翻译页面再次触发 = 还原原文（切换式），按此默认实现，不需要可去掉。
- **扩展自身界面**：首版仅中文，不做 i18n。

## 8. 明确不做（首版）

- 工具栏弹窗、右键菜单入口（选择窗走悬浮按钮）
- 生词本、翻译历史记录
- Firefox / Safari 兼容、图片 OCR 翻译、流式输出
- 多引擎同时使用、手动禁用降级
